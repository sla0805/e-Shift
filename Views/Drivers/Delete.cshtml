@model eShift.Models.Driver

@{
    ViewData["Title"] = "Delete Driver";
}

<h2>Delete Driver</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Driver</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Driver ID</dt>
        <dd class="col-sm-10">@Model.DriverId</dd> @* Updated property name *@

        <dt class="col-sm-2">Driver Name</dt>
        <dd class="col-sm-10">@Model.DriverName</dd> @* Updated property name *@

        <dt class="col-sm-2">License Number</dt>
        <dd class="col-sm-10">@Model.DriverLicensenum</dd> @* Updated property name *@

        <dt class="col-sm-2">Phone</dt>
        <dd class="col-sm-10">@Model.DriverPhone</dd> @* Updated property name *@
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="DriverId" /> @* Updated property name *@
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>